% This function will take an image 
% And the registration parameters
% Convert it to spherical system
%function [ out ] = projectOnSphere(img,R,f)
%out  = img

img = imread('../data/building1.JPG');

[x,y] = size(img);
theta_phi = zeros(2*x,2*y);

for i = 1:x
	for j = 1:y
         
	end
end
